media: media
content:
  - name: posts
    label: Posts
    type: collection
    path: _posts
    filename:
      template: "{year}-{month}-{day}-{primary}.md"
      field: false
    view:
      fields: [ cover, title ]
    fields:
      - { name: cover, label: Cover image, type: image }
      - { name: title, label: Title, type: string, required: true }
      - { name: date, label: false, type: string, default: "{year}-{month}-{day}"}
      - { name: categories, label: Categories, type: select, options: { values: [ "misc", "junk" ] } }
      - { name: author, label: Authors, type: string, list: true }
      - { name: meta, label: Meta, type: string }
      - { name: body, label: Body, type: rich-text }